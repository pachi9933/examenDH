var express = require('express');
var router = express.Router();

const indexController = require('../controllers/indexController') 

/* GET home page. */
//router.get('/', function(req, res, next) {
//  res.render('index', { title: 'express' });
//});

//router.get('/', indexController.show);

//router.get('/',moviesController.all);
//router.get('/detail', moviesController.all);
//router.get('/:id', moviesController.all);
//router.get('/new', moviesController.all);
//router.get('/recomended', moviesController.all);

//router.get('/search', moviesController.all);
//router.post('/search', moviesController.all);


module.exports = router;
